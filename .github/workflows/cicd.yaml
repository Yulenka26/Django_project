name: CI/CD

on:
  push:
    branches:
      - main

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - name: checkout code # получаем весь код
        uses: actions/checkout@v6

      - name: setup uv # устанавливаем uv
        uses: astral-sh/setup-uv@v6
        with:
          python-version: 3.14
          enable-cache: true

      - name: install dependencies # устанавливаем библиотеки
        run: uv sync --frozen

      - name: run linters # запускаем проверку по всем файлам проекта
        run: uv run pre-commit run --all-files

  test:
    needs: lint
    runs-on: ubuntu-latest

    services:
      redis:
        image: redis:alpine
        ports:
          - 6379:6379

    steps:
      - name: checkout code # получаем весь код
        uses: actions/checkout@v6

      - name: setup uv # устанавливаем uv
        uses: astral-sh/setup-uv@v6
        with:
          python-version: 3.14
          enable-cache: true

      - name: install dependencies # устанавливаем библиотеки
        run: uv sync --frozen

      - name: create .env # создаем env файл из шаблона
        run: cp .env.example .env

      - name: run tests
        run: |
          cd src/project
          uv run manage.py test --verbosity=2

  build-and-push:
    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: checkout code # получаем весь код
        uses: actions/checkout@v3

      - name: login to github container registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{github.actor}}
          password: ${{secrets.GITHUB_TOKEN}}

      - name: extract image metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ghcr.io/${{github.repository}}
          tags: |
            type=raw,value=latest
            type=sha,prefix=sha-

      - name: build and push docker image
        uses: docker/build-push-action@v7
        with:
          push: true
          tags: ${{steps.meta.outputs.tags}}
          labels: ${{steps.meta.outputs.labels}}
          cache-from: type=registry,ref=ghcr.io/yulenka26/django_project:latest
          cache-to: type=inline

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest

    steps:
      - name: deploy to vps via ssh
        uses: appleboy/ssh-action@v1
        with:
          host: ${{secrets.SERVER_HOST}}
          username: ${{secrets.SERVER_USER}}
          key: ${{secrets.SERVER_SSH_KEY}}
          port: ${{secrets.SERVER_SSH_PORT}}
          script: |
            cd blog
            docker compose pull
            docker compose up -d
            echo "Деплой завершен"
