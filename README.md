Сделано:
1. Создана CLI‑команда `print_published_posts` с фильтрацией по опубликованным статьям.
3. Создана CLI‑команда `create_post`, которая создаёт новую статью.
4. Создана CLI‑команда `delete_post`, которая удаляет статью по `id`.
5. Создана CLI‑команда `update_post`, которая обновляет название поста.
6. Добавлены все созданные на занятии и в ДЗ команды в `Makefile`
